# Codex 操作记录

- 时间戳：2026-06-30 22:27:56 CST
- 用户目标：说明如何启用程序，并帮助用户现在启动，同时配置开机自启动。
- 已阅读的重要文件：
  - `README.md`
  - `Package.swift`
  - `Sources/CodexTopGuageMac/main.swift`
  - `computer-use` 插件技能说明
- 做过的修改：
  - 新增 LaunchAgent 配置：`launchd/com.kenneth2y.codextopguage.mac.plist`
  - 新增安装脚本：`scripts/install-launch-agent.sh`
  - 新增卸载脚本：`scripts/uninstall-launch-agent.sh`
  - README 增加中英双语的开机自启动说明。
- 关键决策与原因：
  - 使用 macOS `LaunchAgent` 而不是图形界面手动设置，便于自动化、复现和卸载。
  - LaunchAgent 目标为当前用户会话 `gui/501`，避免使用系统级 root daemon。
  - plist 使用当前 release 可执行文件的绝对路径，适合源码仓库当前阶段。
- 未完成事项 / 风险 / 后续建议：
  - 如果移动项目目录，需要重新运行安装脚本。
  - 后续正式发布 `.app` 后，应将 LaunchAgent 指向 app bundle 或改用 Login Items。
